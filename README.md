# github-scraper

Scrape a GitHub repo for stars, forks, watching, releases, used and contributors stats.

## Library

This is a Rust library which exposes a function which takes the URL of a GitHub repo and returns above stats.

It also exposes a C ABI function so that this library could be used from C# or Python.
